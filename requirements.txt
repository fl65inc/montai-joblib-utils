# Runtime dependencies for AgentCore CodeZip build.
# Keep in sync with [project.dependencies] in pyproject.toml.
strands-agents>=1.47.0
mcp>=1.28.1
boto3>=1.43.0
pydantic>=2.0
fastapi>=0.139.2
uvicorn[standard]>=0.51.0
aws-opentelemetry-distro>=0.10.0
